# Build directories
build/
build-*/
cmake-build-*/

# Runtime database files: the schema and data are generated by the demo, never
# committed. The rubric asks for evidence of their size on disk, not for the
# files themselves.
data/*.db
data/*.db-*

# Compilation database and editor state
compile_commands.json
.cache/
.vscode/
.idea/

# Packaged deliverable
minidb-src.zip
