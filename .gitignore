# Build directories
build/
build-*/
cmake-build-*/

# Runtime database files: the schema and data are generated by the demo, never
# committed. The rubric asks for evidence of their size on disk, not for the
# files themselves.
data/*.db
data/*.db-*

# Compilation database and editor state
compile_commands.json
.cache/
.vscode/
.idea/

# Packaged deliverable
minidb-src.zip

# Datos y bases de datos generados por los experimentos. Los CSV y las figuras de
# experimentos/resultados y experimentos/graficos SÍ se versionan: son la evidencia
# del artículo.
experimentos/datos/datos_generados/
